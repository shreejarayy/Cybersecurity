# config.py
# api keys and settings — add to .gitignore, never push this

# github personal access token — gives 5000 api requests/hour instead of 60
# generate one at: github.com → settings → developer settings → personal access tokens
# only needs "public_repo" scope for scanning public repos
GITHUB_TOKEN = ""
